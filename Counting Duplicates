function duplicateCount(text){
  //...
  const caseSens = text.toLowerCase().split("");
  const originalArray = [...new Set (caseSens)]
  const uniquestr = originalArray
  let count =0;
  for(let val of uniquestr){
   if(caseSens.filter(v=>v===val).length>1){
   count++;
}
}
return count;
}
